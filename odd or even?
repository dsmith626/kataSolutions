function oddOrEven(array) {
   let sum = 0;
    for (let i = 0; i < array.length; i++) {
      let currentNum = array[i];
      sum += currentNum;
    }
    if (sum % 2 === 0) {
       return "even";
    } else {

    return 'odd';
    }
}
